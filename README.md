# comodoro

Pomodoro Timer in C language.

Dependencies:

- `SDL2` for gui
- `SDL2_Mix` for audio

## Compile and Usage

To compile just:

```
make
```

To run:

```
./comodoro <number of sections> <time of each section> <relax time>
```

